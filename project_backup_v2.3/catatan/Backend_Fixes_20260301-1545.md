# Backend Fixes: Type Hinting in Boards Endpoint

**Tanggal:** 2026-03-01
**Status:** Completed
**Author:** AI Assistant

## Ringkasan Perubahan
Memperbaiki isu type mismatch yang terdeteksi oleh linter pada file `app/api/endpoints/boards.py`. Isu ini terjadi karena `decrypt_password` mengharapkan tipe `str`, namun menerima `Column[str]` dari model SQLAlchemy.

## Komponen yang Dimodifikasi
- **`app/api/endpoints/boards.py`**
  - Menambahkan casting eksplisit `str()` pada `cred.password_mikrotik_encrypted` sebelum dipassing ke `decrypt_password`.
  - Lokasi perubahan:
    - Endpoint `read_board_interfaces`
    - Endpoint `read_board_pppoe`
    - Endpoint `read_board_hotspot`

## Manfaat Teknis
1.  **Code Quality:** Menghilangkan warning/error dari static type checker.
2.  **Robustness:** Memastikan nilai yang dipassing ke fungsi enkripsi adalah string primitive, menghindari potensi ambiguitas runtime.

## Kepatuhan Rules
- **Layer 1 (Governance):** Severity LOW (Refactor minor).
- **Layer 2 (Backend Stack):** Tidak mengubah stack, hanya perbaikan logika kecil.
