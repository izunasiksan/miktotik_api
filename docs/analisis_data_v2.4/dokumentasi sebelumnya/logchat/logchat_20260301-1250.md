# LOGCHAT - Linter Fixes (Security Core)
Date: 2026-03-01

[12:50] - [BACKEND] - **FIXED** `security.py` linter errors:
- **Type Mismatch (Parameter)**: Fixed `Expression of type "None" cannot be assigned to parameter of type "timedelta"`.
  - **Change**: Updated `create_access_token` signature to `expires_delta: timedelta | None = None`.
- **Type Mismatch (Return)**: Fixed `Type "None" is not assignable to return type "dict"`.
  - **Change**: Updated `decode_access_token` signature to `-> dict | None`.
  - **Reasoning**: The function returns `None` when `jwt.ExpiredSignatureError` or `jwt.InvalidTokenError` occurs, so the return type must explicitly include `None`.

[12:51] - [SYSTEM] - **VERIFICATION**:
- Confirmed type hints are compatible with Python 3.11+ syntax (`| None`).
- Verified that `create_access_token` correctly handles optional `expires_delta`.
- Verified that `decode_access_token` return type accurately reflects its behavior.

[12:52] - [DECISION] - **NEXT STEPS**:
- Continue monitoring for linter errors in other modules.
