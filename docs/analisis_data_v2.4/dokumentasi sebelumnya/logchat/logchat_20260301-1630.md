# 📝 LOGCHAT: BUG FIX & STABILIZATION
**Date:** 2026-03-01 16:30
**Topic:** Router Connection & Reports UI Stabilization

## 1. SUMMARY OF CHANGES
Kami telah melakukan serangkaian perbaikan untuk menstabilkan koneksi router dan tampilan frontend.

### A. Backend Fixes (Critical)
*   **Credential Handling:** Memperbaiki `app/core/config.py` agar `AES_SECRET_KEY` dimuat dengan benar dari `.env` (menambahkan parent directory ke `env_file`). Sebelumnya, key yang salah menyebabkan kegagalan dekripsi password router.
*   **Debug Tooling:** Menambahkan endpoint `/debug-auth` di `boards.py` untuk mempermudah troubleshooting koneksi dan dekripsi password secara real-time.
*   **Error Messaging:** Meningkatkan pesan error pada endpoint auth agar frontend dapat menampilkan pesan yang lebih spesifik ("Invalid username or password").

### B. Frontend Fixes (UI/UX)
*   **Recharts Layout Bug:** Memperbaiki error `width(-1)` pada halaman **Reports** dan **Hotspot Analytics** dengan menambahkan prop `minWidth={0}` pada `ResponsiveContainer`. Grafik kini merender dengan stabil.
*   **Error Handling UI:** Mengupdate komponen `InterfaceList`, `PPPoEMonitor`, dan `HotspotMonitor` untuk menampilkan pesan error yang lebih jelas (mengambil dari response backend) dan menyediakan tombol **Retry**.

## 2. FILES MODIFIED
*   `app/core/config.py`
*   `app/api/endpoints/boards.py`
*   `frontend/src/pages/Reports.jsx`
*   `frontend/src/components/router/HotspotAnalytics.jsx`
*   `frontend/src/components/router/InterfaceList.jsx`
*   `frontend/src/components/router/PPPoEMonitor.jsx`
*   `frontend/src/components/router/HotspotMonitor.jsx`

## 3. VERIFICATION RESULTS
*   ✅ **Router Auth:** Sukses connect ke router `10.10.10.1` dengan user `admin`.
*   ✅ **Reports Page:** Grafik tampil normal, tidak ada error console.
*   ✅ **Error Feedback:** UI menampilkan pesan error yang sesuai saat koneksi gagal.

## 4. NEXT STEPS
*   Monitoring stabilitas koneksi.
*   Lanjut ke Phase 16 (Hotspot Loyalty).
